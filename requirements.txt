# ===========================
# Trading-bot - Requirements
# ===========================
# Python ≥ 3.10
# Compatible macOS / Linux / Windows
# -----------------------------------

# --- Core Python scientific stack ---
numpy>=1.26.0
pandas>=2.2.2
scipy>=1.12.0

# --- Data I/O & Storage (local) ---
pyarrow>=15.0.0        # lecture/écriture de fichiers .parquet
fastparquet>=2024.1.0  # alternative parquet plus rapide en fallback
python-dotenv>=1.0.1   # lecture des variables d'environnement (.env)
requests>=2.31.0       # requêtes HTTP génériques
tenacity>=8.2.3        # gestion des retries pour API OANDA
tqdm>=4.66.0           # barres de progression CLI

# --- Trading & backtesting ---
# Pas de framework externe lourd : moteur maison.
# Mais certains outils peuvent aider :
ta>=0.11.0             # indicateurs techniques standards (RSI, EMA, ATR…)


# --- Optimisation & calcul ---
joblib>=1.4.0          # parallélisation de grille d'optimisation
numba>=0.59.0          # accélération possible des boucles (facultatif)
scikit-optimize>=0.10.1  # si tu veux pousser l’optimisation bayésienne

# --- Visualisation & rapports ---
matplotlib>=3.9.0
seaborn>=0.13.2
plotly>=5.23.0         # pour les rapports interactifs (optionnel)
tabulate>=0.9.0        # jolis tableaux en console
rich>=13.7.1           # affichage CLI coloré / logs enrichis

# --- Divers utiles ---
pytz>=2024.1
dateparser>=1.2.0      # parsing flexible des dates CLI

# --- Tests & lint (dev only) delete for live ---
pytest>=8.2.0
pytest-cov>=5.0.0
black>=24.4.2
ruff>=0.4.8
mypy>=1.10.0